import { Badge } from './ui/Badge';

/**
 * Badge component to identify audience members in participant lists
 */
export function AudienceMemberBadge() {
  return (
    <Badge variant="primary" size="sm">
      Audience
    </Badge>
  );
}
