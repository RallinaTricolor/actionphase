# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Working copy of backend/pkg/docs/openapi.gen.yaml, staged here by
# `just gen-api-types` because this container mounts only ./frontend (no /repo).
# Never commit it — a second copy of the spec is exactly the drift this generator
# exists to eliminate.
.openapi.gen.yaml

# Playwright
test-results/
playwright-report/
playwright/.cache/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
