Treat this whole file as a prompt. Do not edit this file. Below is a list of issues that need to be addressed.
All of these issues were tested by other users in the staging environment.

Create a new file to track your work on these issues. For each issue in the list, create a task to track your work. This task
should consist of the following information:
- What the issue is
- A summary of the investigation/reproduction of the issue
- The status of your fix
- Any tests that need to be created or updated
- Any other notes you want to make about the issue

- Better error feedback on registration/login
    - Password length, validation, etc.
    - Currently users just see raw error responses throughout most of the app
- Token expiration is weird
  - JWT + Auth Cookie are stored at the same time--is this wise?
  - Had an issue where I was logged out with no error message in staging with no explanation
- Character creation shows for non-participants -- it should not
- Re-approve applicant doesn't seem to be working
    - What happens to the characters if a player makes a billion characters. Does the GM see them all all the time?
    - Maybe we need to bring back "Rejected"
- Dashboard card unread count doesn't refresh without hard refresh
- Reply button even if not a participant in the common room threads
- Can't vote with multiple characters if the poll allows for it
- Can't reply to deeply nested comments (no Reply button on the comment with the Continue this thread link)
- Load more button in case of too many top level replies
  - Right now we load ALL replies to a common room post, even if there are hundreds
- Can send PMs outside of common room
  - Direct messaging should only be available during an active common room
- Can't look at previous polls in history tab
