- Notification sound effect
- USER AND API DOCS
- Published results and:
  - Player didn't get a notification
- Full Game Flow Notes
  - Deadlines header visible in "Setup" and "Recruiting" state--should be Character Creation / In Progress *only*
  - Can't edit anything in "Setup" state -- should have Handouts tab available to start writing content, maybe People
    - Handouts needs to be available in every state for both GMs and players
  - "Has Open Spots" filter shows games that aren't recruiting
  - "Anonymous Mode" and "Auto Accept Audience" weren't persisted from game creation
  - Game editing allowed me to enable "Anonymous Mode" but NOT "Auto Accept Audience"
    - Request was correct (all data sent)
  - "Applied" game filter doesn't show games that the user has applied to
  - "Applications" tab shouldn't be visible after recruitment ends
  - UI for approving applications is a bit rough--the UI moves around a lot after hitting "Approve"
  - No notification for "Accepted" to game once character creation starts
  - "Close Recruitment" button isn't clear that it is starting Character Creation
    - Edit button is always first, phase transitions should be above Edit Game
  - "Participants" and "Characters" tab shown during character creation rather than unified "People" tab
  - Player can create their own character, but can't see it while it's pending (they should be able to)
  - Audience member cannot see pending characters (should be able to see all)
  - In "Anonymous Mode" players cannot edit their own character and no "Your Character" badge
  - Deadline display doesn't show descriptions, the emoji isn't useful and cuts off title text (which is already limited)
  - Polls shouldn't be its own tab, it should only be associated with Common Rooms
  - Common Room Deadline isn't visible in the deadlines section, it should be
  - GM replying as an NPC, can we default to the parent comment's author if available?
  - GM creating a conversation can't see their own NPCs to add (should be able to for group chat sake)
  - In Audience view, if the GM replies to a private conversation as 2 different characters in quick succession, it looks like they came from the same character
  - Private conversation view still feels too small for longer conversations. The conversation window is just very small in terms of screen real estate
  - When updating character sheet as part of a pending result, the form for adding a new ability, skill, or item is different than normal
    - Adding currency is still unclear if you are setting, adding or subtracting
  - Remove the "Publish Immediately" checkbox from the "Results" tab
  - Pending character sheet updates were not applied on publish (individual or publish all)
  - The GM should not see "Recent Action Results"
  - You can still edit/delete comments in the "History" view of a completed game
    - History tab should be completely read only, regardless of Game State
  - Poll datepicker is non-standard compared to the rest of the app
  - GM can vote on polls (shouldn't be able to)
  - Poll results are visible to players before the poll ends
    - Shows "Not Voted" despite having voted
    - GM should be able to see individual results
    - GM should be able to see "Other" custom responses
    - Audience cannot see active "Polls" at all
    - Poll deadline not in the deadlines section
